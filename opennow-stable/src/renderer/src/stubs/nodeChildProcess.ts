// Stub: node:child_process — not used in browser build
export const spawn = (): unknown => ({ on: () => {}, stdout: null, stderr: null });
export const exec = (_cmd: string, _cb?: unknown): unknown => null;
export default { spawn, exec };
